# Pin CocoaPods for EAS iOS builds. If a Gemfile is present, EAS runs
# `bundle install` and uses `bundle exec pod install`, ensuring the `pod`
# binary is available in the macOS build environment.
source "https://rubygems.org"

gem "cocoapods", "~> 1.15.2"
